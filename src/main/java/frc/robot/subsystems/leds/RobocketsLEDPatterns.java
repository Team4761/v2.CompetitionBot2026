package frc.robot.subsystems.leds;

import edu.wpi.first.wpilibj.LEDPattern;
import edu.wpi.first.wpilibj.util.Color;

/**
 * This stores all of our LED patterns.
 * <p> note from Alex, the LED guy:
 * <p> I am not currently using this. If I need to, I might migrate patterns from LEDSubsystem. But that's a lot of unnecassary work, considering how some of them are run.
 */
public class RobocketsLEDPatterns {
    public static final LEDPattern OFF = LEDPattern.solid(Color.kBlack);
}
